language: ruby
rvm:
  - 2.6.3
  - 2.5.5
  - 2.4.6
env:
  - RAILS=4.2.11.1 GPG_BIN=/usr/bin/gpg1
  - RAILS=5.2.3 GPG_BIN=/usr/bin/gpg1
matrix:
  exclude:
before_install:
  - gem update bundler
  - sudo apt-get install -y gpg1
sudo: false
cache: bundler
addons:
  apt:
    update: true
